# Base URL used to generate clickable approval links
BASE_URL=http://localhost:4567

# SQLite DB file path
DB_PATH=./db/dev.sqlite3

# Default coordinator email if none is provided on job creation
COORDINATOR_EMAIL_DEFAULT= (ENTER YOUR EMAIL HERE)

# Offer window before timing out an offer (minutes)
OFFER_TIMEOUT_MINUTES=30

# Sending mode: "fake" prints to console. Later you can swap to "twilio".
DISPATCHER_MODE=fake
